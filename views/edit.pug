-let newTaskDescription = taskDescription
<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Edit
    body
        form(action=`/edit_task/${index}` method="post") 
            input(type="text" name="newTaskValue" value=newTaskDescription)
            button(type="submit") update